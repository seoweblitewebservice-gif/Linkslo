import type { ToolEditorial } from "@/lib/seo-tools/content";

export const SITEMAP_TOOL_EDITORIAL: Record<string, ToolEditorial> = {
  "sitemap-validator": {
    definition: "An XML sitemap validator checks whether a sitemap is structured as a valid sitemap document rather than merely confirming that the URL ends in .xml. It looks for the expected `<urlset>` or `<sitemapindex>` root, usable `<loc>` values, recognizable XML and common formatting problems that can prevent search engines from reading the file consistently. A sitemap can be reachable in a browser and still contain malformed entries, stale hosts or values that do not belong in the file.",
    whyItMatters: "Sitemaps are discovery files, not ranking boosters, but they become especially important on large sites, new sites and websites with URLs that are difficult to reach through ordinary internal links. If the file contains malformed XML or non-canonical URLs, it sends weak or contradictory discovery signals. Validation is therefore less about earning an SEO score and more about confirming that the file does the job it was created to do.",
    howToUse: ["Enter the full sitemap URL, including the protocol.", "Run the validation and confirm the server returns a successful response rather than an HTML error page disguised as XML.", "Review the detected root type and the number of `<loc>` values.", "Inspect any invalid or non-HTTP URLs and compare them with the site's canonical URL rules.", "After fixing the file, retest it and then submit the final sitemap through your preferred search-engine webmaster tools if needed."],
    interpretation: ["A `<urlset>` root means the file contains page URLs directly.", "A `<sitemapindex>` root means the file is an index pointing to child sitemaps rather than page URLs.", "A valid `<loc>` should normally contain an absolute HTTP or HTTPS URL.", "Successful validation does not mean every listed URL is indexable; the sitemap only tells crawlers which URLs you want discovered."],
    commonProblems: ["Returning an HTML 404 template from a URL named sitemap.xml.", "Mixing staging and production hostnames in `<loc>` values.", "Listing redirected URLs instead of final canonical destinations.", "Using malformed entities such as raw ampersands inside URLs.", "Keeping deleted pages in the sitemap long after they stop being useful."],
    fixes: ["Generate sitemap XML from canonical production URLs rather than request logs or database records with mixed hosts.", "Escape XML characters correctly and make sure every entry is an absolute URL.", "Remove redirected, noindex and intentionally blocked URLs from ordinary indexable-page sitemaps.", "Split very large files into child sitemaps and reference them from a sitemap index."],
    bestPractices: ["Keep sitemap generation close to the application's canonical routing logic.", "Rebuild or update sitemaps when URLs are created, removed or permanently redirected.", "Use separate sitemaps when grouping by content type helps diagnostics, but do not create dozens of tiny files without a reason.", "Monitor submitted sitemap reports for discovered versus indexed URLs, remembering that submission does not guarantee indexing."],
    example: "A valid ecommerce sitemap should normally list the clean canonical product URL, not tracking versions such as `?utm_source=email` and not an old product URL that immediately 301 redirects to a new slug. If the store has hundreds of thousands of products, a sitemap index can point to multiple product sitemap files instead of forcing everything into one document.",
    limitations: "This validator checks the sitemap document it can retrieve now. It does not prove that Google or another search engine has processed the file, discovered every URL, or chosen to index those URLs. Search Console and other first-party webmaster platforms remain the better source for search-engine-specific processing status.",
    faqs: [
      { question: "Does a valid sitemap guarantee indexing?", answer: "No. It improves URL discovery, but search engines still evaluate crawlability, canonicalization, content and many other signals before indexing." },
      { question: "Can a sitemap contain redirected URLs?", answer: "It can technically, but a clean sitemap should normally list final canonical URLs rather than addresses that redirect." },
      { question: "Should noindex pages appear in an XML sitemap?", answer: "Usually no. A sitemap intended for indexable content should not simultaneously list URLs that explicitly request noindex." },
      { question: "Can I submit more than one sitemap?", answer: "Yes. Large or complex sites often use multiple child sitemaps and a sitemap index." },
      { question: "Does the file have to be named sitemap.xml?", answer: "No. Search engines can use other filenames as long as the sitemap is reachable and correctly referenced or submitted." },
      { question: "What happens if my sitemap returns HTML?", answer: "Search engines may fail to parse it as a sitemap even if the URL itself returns a 200 status." },
    ],
  },
  "sitemap-url-checker": {
    definition: "A Sitemap URL Checker goes one step beyond XML validation by examining URLs listed inside the sitemap and sampling their HTTP behavior. The important distinction is that a structurally correct sitemap can still contain bad destinations: redirected pages, 404s, expired content, mixed protocols or URLs that no longer match the site's canonical structure.",
    whyItMatters: "Sitemap cleanliness is useful for diagnosis. If a sitemap is meant to represent canonical indexable pages, then a large number of redirects or errors inside that file makes it harder to understand what the site actually wants crawlers to discover. On migrations, sitemap URL checking is also a quick way to catch old-domain entries that survived the release.",
    howToUse: ["Enter the XML sitemap URL rather than a normal webpage.", "Let the tool parse `<loc>` entries and safely sample a limited number of destinations.", "Review 4xx and 5xx errors first, then inspect redirects.", "Compare any redirected source with the final URL and update the sitemap generator if the redirect is permanent.", "For very large sitemaps, treat the sample as a diagnostic and use your own crawler or logs for full-scale verification."],
    interpretation: ["A successful sampled URL means the destination responded without an HTTP error during the check.", "A redirect warning means the sitemap is listing an intermediate URL rather than the final response seen by the checker.", "A request failure can reflect DNS, SSL, timeout or server blocking and should be investigated separately.", "The total URL count describes the sitemap; the tool intentionally samples a limited number of destinations to avoid becoming an unrestricted crawler."],
    commonProblems: ["Sitemap generator still using HTTP while the site is HTTPS-only.", "Old slugs remain listed after permanent redirects were added.", "Product or article URLs return 404 after being removed from the CMS.", "Localized sitemap contains URLs from the wrong language host.", "Sitemap points to pages behind authentication or bot protection."],
    fixes: ["Update the sitemap source so it emits the current canonical URL directly.", "Remove deleted content unless there is a real replacement that should be listed instead.", "Fix application routing before repeatedly resubmitting a sitemap with failing destinations.", "Separate public indexable content from account, checkout and utility URLs that do not belong in organic discovery."],
    bestPractices: ["Check sitemap destinations after every major migration.", "Compare sitemap URLs with canonical tags and internal navigation.", "Keep sitemaps focused on URLs you genuinely want crawled and considered for indexing.", "Use server logs or search-engine crawl reports to understand how often sitemap URLs are actually requested."],
    example: "Suppose a sitemap lists `/blog/old-title/`, which returns a 301 to `/guides/new-title/`. The redirect may work perfectly for users, but the sitemap should usually be updated to list `/guides/new-title/` directly so discovery signals agree with the current architecture.",
    limitations: "The tool samples a limited number of URLs per run for performance and abuse protection. It cannot replace a full site crawler for a sitemap containing tens of thousands of URLs, and a successful response does not prove search-engine indexing.",
    faqs: [
      { question: "Why does the checker sample instead of requesting every sitemap URL?", answer: "Unrestricted bulk fetching can be slow and abusive. Sampling provides useful diagnostics while keeping the tool safe and responsible." },
      { question: "Is a 301 URL bad in a sitemap?", answer: "The redirect itself can be valid, but sitemaps should normally list the final canonical URL directly." },
      { question: "Should 404 URLs stay in a sitemap so Google learns they are gone?", answer: "No. Remove them from the sitemap. Search engines can discover the 404 through normal recrawling and links." },
      { question: "Can sitemap URLs be noindex?", answer: "They can, but that sends conflicting intent. Indexable sitemaps should generally avoid deliberate noindex URLs." },
      { question: "What if the server blocks this checker?", answer: "The tool will report the fetch failure rather than inventing a status. Check server or firewall rules separately." },
    ],
  },
  "sitemap-index-checker": {
    definition: "A sitemap index is an XML file whose job is to point crawlers toward other sitemap files. A Sitemap Index Checker verifies that the document actually uses `<sitemapindex>`, extracts child `<loc>` values and highlights duplicate or malformed child sitemap references. It is especially useful on large sites where several content-specific sitemap files are generated independently.",
    whyItMatters: "A broken child reference can make entire sections harder to discover even when the index file itself looks fine. Sitemap indexes also tend to accumulate stale child files after CMS migrations, language restructures or content-type changes. Checking the index helps teams see whether the map of maps still represents the live site.",
    howToUse: ["Enter the sitemap index URL.", "Confirm the root is `<sitemapindex>` rather than `<urlset>`.", "Review the child sitemap URLs and look for duplicates or unexpected hosts.", "Open or validate suspicious child sitemaps individually.", "Update the index generator so removed child files disappear automatically rather than relying on manual edits."],
    interpretation: ["A valid sitemap index contains one or more `<sitemap>` entries with `<loc>` child URLs.", "Duplicate child URLs are unnecessary and can indicate generator bugs.", "A normal sitemap with `<urlset>` is not wrong by itself; it is simply not a sitemap index.", "Lastmod on sitemap entries should describe meaningful modification of the child sitemap when used."],
    commonProblems: ["Index file references deleted child sitemaps.", "Child sitemap URLs redirect across protocol or hostname changes.", "Same child file listed more than once.", "Index mixes staging and production sitemap hosts.", "A framework accidentally places page URLs directly inside `<sitemapindex>`."],
    fixes: ["Regenerate the index from the same production configuration that generates child files.", "Remove stale references and point directly to final child sitemap URLs.", "Use `<urlset>` for page-URL files and `<sitemapindex>` only for files that reference other sitemaps.", "Keep child sitemap naming predictable enough for monitoring and debugging."],
    bestPractices: ["Group sitemaps in ways that help diagnosis, such as articles, products or locales.", "Do not create artificial category splits solely to produce more sitemap files.", "Validate newly added child files before adding them to the production index.", "Monitor index and child files after deploys because generation can fail at different layers."],
    example: "A multilingual marketplace might publish `sitemap-products-en.xml`, `sitemap-products-fr.xml` and `sitemap-guides.xml`, with a single sitemap index pointing to all three. If the French section is retired, the child file and its index entry should both be removed deliberately.",
    limitations: "The checker focuses on index structure and listed child URLs. It does not guarantee that every child sitemap is fully valid unless those files are checked separately.",
    faqs: [
      { question: "What is the difference between a sitemap and sitemap index?", answer: "A sitemap lists content URLs. A sitemap index lists other sitemap files." },
      { question: "Can a sitemap index contain another sitemap index?", answer: "Search-engine support and protocol expectations should be checked carefully; keep structures simple and use child sitemap files directly where possible." },
      { question: "How many child sitemaps should I use?", answer: "Use as many as needed for scale and maintainability rather than targeting an arbitrary count." },
      { question: "Should child sitemap URLs redirect?", answer: "Prefer direct final sitemap URLs to avoid unnecessary request hops." },
      { question: "Does every child sitemap need lastmod?", answer: "No. Use lastmod only when you can maintain a meaningful modification date." },
    ],
  },
  "sitemap-lastmod-checker": {
    definition: "The `<lastmod>` field in an XML sitemap is meant to describe when a URL's content was meaningfully modified. A Sitemap Lastmod Checker reviews the date strings used in a sitemap, flags formatting that does not resemble accepted ISO-style values and highlights future dates that deserve investigation.",
    whyItMatters: "Lastmod becomes less useful when every URL is stamped with the deployment time or current date regardless of whether content changed. Search engines can learn to distrust noisy timestamps. Accurate modification data is more informative than simply filling the field because a sitemap template supports it.",
    howToUse: ["Enter a sitemap URL containing page entries or child sitemap entries.", "Review how many lastmod values are present.", "Inspect invalid or future-dated values.", "Compare a few timestamps with real content modification history in your CMS or database.", "If your system cannot produce meaningful modification dates, consider omitting lastmod instead of fabricating freshness."],
    interpretation: ["A date such as `2026-09-18` is a simple ISO-style date.", "A timestamp can include time and timezone information when needed.", "Missing lastmod is not automatically an error.", "A future date may be caused by timezone mistakes, scheduled publishing or incorrect system clocks and should be checked."],
    commonProblems: ["Every sitemap URL receives today's date on every request.", "CMS publish date is used even when the page has been materially updated later.", "Locale or server timezone causes tomorrow's date to appear prematurely.", "Invalid text or inconsistent date formatting is inserted into XML."],
    fixes: ["Use real content-modification data where the application has it.", "Normalize timestamps to a consistent ISO-compatible format.", "Avoid resetting lastmod merely because the sitemap file itself was regenerated.", "Remove future dates unless the content genuinely has a future-effective timestamp and the use is intentional."],
    bestPractices: ["Treat lastmod as factual metadata rather than an SEO freshness lever.", "Update it for substantial content changes, not every minor template render.", "Keep timezones explicit when using full timestamps.", "Audit generator logic after CMS migrations because modification fields often change meaning between platforms."],
    example: "If an article was published in March and substantially revised in September, a September lastmod can be meaningful. If the page has not changed since March but the sitemap says today every morning, the field is providing noise instead of useful information.",
    limitations: "The checker can validate formatting and obvious future dates, but it cannot know whether a timestamp reflects a genuinely meaningful content change without access to your editorial history.",
    faqs: [
      { question: "Is lastmod required in XML sitemaps?", answer: "No. It is optional." },
      { question: "Should lastmod equal the sitemap generation time?", answer: "Not automatically. It should describe meaningful modification of the referenced URL or sitemap entry." },
      { question: "Can lastmod include time?", answer: "Yes. ISO-style datetime values can include time and timezone information." },
      { question: "Does changing lastmod force recrawling?", answer: "No. Crawlers decide when to revisit URLs, and unreliable timestamps may be discounted." },
      { question: "What counts as a meaningful page update?", answer: "Substantive changes to the primary content are more meaningful than routine template or timestamp changes." },
    ],
  },
  "sitemap-generator": {
    definition: "An XML Sitemap Generator converts a deliberate list of absolute URLs into a standards-compliant `<urlset>` document. The useful part is not the XML syntax itself; it is the discipline of deciding which URLs deserve to appear as canonical discovery targets and excluding noise such as tracking versions, account pages and redirects.",
    whyItMatters: "Small websites can work perfectly without hand-crafted sitemaps, but a clean generated sitemap gives crawlers a direct list of preferred URLs and gives site owners a practical auditing surface. It is especially helpful when content is new, deep in navigation or published at scale.",
    howToUse: ["Prepare a list of complete canonical HTTP or HTTPS URLs, one per line.", "Remove duplicates, redirected addresses and URLs you intentionally noindex.", "Generate the XML and save it with an appropriate `.xml` filename.", "Publish the file on your website and confirm it returns XML with a successful response.", "Reference it in robots.txt or submit it through search-engine webmaster tools when useful."],
    interpretation: ["Each `<url>` entry contains one `<loc>` identifying a discoverable URL.", "The generator escapes XML-sensitive characters in URLs.", "The output intentionally avoids fake priority or changefreq values because those should not be invented.", "You can extend the file with accurate lastmod data if your system has reliable modification timestamps."],
    commonProblems: ["Generating a sitemap from every database row without checking public routing.", "Including parameter variations and tracking URLs.", "Listing both HTTP and HTTPS versions of the same page.", "Adding noindex utility pages because they exist in the router.", "Manually maintaining a file that quickly becomes stale."],
    fixes: ["Build the URL list from canonical route data or your production CMS.", "Deduplicate before generation and normalize host/protocol conventions.", "Automate sitemap generation when the content set changes frequently.", "Validate the published file and sample its URLs after deployment."],
    bestPractices: ["List URLs you genuinely want search engines to discover and consider for indexing.", "Use a sitemap index when scale requires multiple files.", "Keep the sitemap on the same canonical host unless you have a deliberate cross-host setup.", "Regenerate after migrations and check that legacy hosts disappeared."],
    example: "A five-page brochure website can have a sitemap containing the homepage, services, about, contact and one indexable resource page. It should not include `/login`, tracking-parameter variants or a `/thank-you` page that is intentionally noindex.",
    limitations: "The generator works from the URLs you provide. It cannot know whether a URL is canonical, indexable or high quality unless you check those pages separately.",
    faqs: [
      { question: "Do I need priority and changefreq?", answer: "No. They are optional, and there is little value in inventing values without a meaningful source." },
      { question: "How many URLs can one sitemap contain?", answer: "The sitemap protocol has size and URL-count limits. Very large sites should split URLs across child sitemaps." },
      { question: "Can I include images or videos?", answer: "Specialized sitemap extensions exist, but this generator focuses on the core URL sitemap format." },
      { question: "Should I include redirected URLs?", answer: "Prefer the final canonical destination directly." },
      { question: "Can my sitemap live on another domain?", answer: "Advanced cross-site setups exist, but most sites should keep sitemap ownership and host relationships straightforward." },
    ],
  },
};
