import type { ToolDepth } from "@/lib/seo-tools/depth-technical";

/** Optional dedicated depth overrides. Main path uses fallbackDepth in depth.ts */
export const EXTRA_TOOL_DEPTH: Record<string, ToolDepth> = {};
